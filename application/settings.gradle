rootProject.name = 'pao-java-skelton-spring'

include ':applications'
include ':src:communication'
include ':src:shared'
include ':src:sharedtest'

buildCache {
	local {
		directory = "/build-cache"
	}
}